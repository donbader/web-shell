# Web Shell Environment Configuration

# ========================================
# Backend Environment Selection
# ========================================
# Choose which backend environment to build/run
# Options: minimal, default
# - minimal: Bare essentials (200MB, fast startup)
# - default: Full-featured with tools (240MB, enhanced shell)
BACKEND_ENVIRONMENT=default

# ========================================
# Application Settings
# ========================================
# Authentication
AUTH_ENABLED=false

# CORS Origins (comma-separated for multiple)
CORS_ORIGINS=http://localhost:3377

# Session Limits
MAX_SESSIONS_PER_USER=5
IDLE_TIMEOUT_MINUTES=30

# ========================================
# Frontend Build Settings (Production)
# ========================================
VITE_API_URL=http://localhost:3366
VITE_WS_URL=ws://localhost:3366

# ========================================
# Build Examples
# ========================================
# Build with minimal environment:
#   BACKEND_ENVIRONMENT=minimal docker compose build
#
# Build with default environment:
#   BACKEND_ENVIRONMENT=default docker compose build
#   (or just: docker compose build)
#
# Run with specific environment:
#   BACKEND_ENVIRONMENT=minimal docker compose up
